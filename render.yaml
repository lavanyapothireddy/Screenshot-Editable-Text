services:
  - type: web
    name: screenshot-to-text
    runtime: static
    buildCommand: ""
    staticPublishPath: "."
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
