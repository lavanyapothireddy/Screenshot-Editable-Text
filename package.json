{
  "name": "screenshot-to-text",
  "version": "1.0.0",
  "description": "Screenshot to Editable Text Tool — Full Stack",
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build && cd ..",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "groq-sdk": "^0.9.1",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
